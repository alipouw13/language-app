# ============================================================================
# Language Learning App — Backend Configuration
# Copy to .env and fill in real values. NEVER commit .env to git.
# All Azure services use Entra (Azure AD) authentication.
# ============================================================================

# --- PostgreSQL (Entra authentication for Azure, password for local) ---
# For Azure: use your Entra UPN as username, password is ignored (token used)
DATABASE_URL=postgresql+asyncpg://user@domain.onmicrosoft.com:placeholder@your-server.postgres.database.azure.com:5432/language_app
# For local dev:
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/language_app

# --- Azure OpenAI (Entra authentication) ---
# API key is not used - Entra DefaultAzureCredential is used instead
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-02-15-preview
AZURE_OPENAI_DEPLOYMENT=gpt-4

# --- Azure Speech Services (Entra authentication) ---
AZURE_SPEECH_ENDPOINT=https://<region>.api.cognitive.microsoft.com/
AZURE_SPEECH_REGION=eastus

# --- Azure AI Foundry endpoints (alternative speech models) ---
AZURE_FOUNDRY_ENDPOINT=https://<foundry-resource>.cognitiveservices.azure.com/openai/
AZURE_SPEECH_TO_TEXT_KEY=your-api-key
AZURE_SPEECH_TO_TEXT_MODEL_NAME=whisper
AZURE_TEXT_TO_SPEECH_MODEL_NAME=tts-1

# --- App ---
CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
LOG_LEVEL=INFO
