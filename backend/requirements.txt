# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Multipart form parsing (required for audio upload / STT endpoint)
python-multipart>=0.0.9

# Fabric OneLake Lakehouse storage (Delta tables)
deltalake>=0.18.2,<0.25
pyarrow>=15.0.0

# Azure OpenAI / Foundry (chat, translation, speech models)
openai>=1.30.0

# Azure Identity (Entra authentication for OpenAI, Foundry, OneLake)
azure-identity>=1.15.0

# Entra ID access-token validation (backend JWT/JWKS)
PyJWT[crypto]>=2.8.0

# Environment
python-dotenv>=1.0.0

# WebSockets (included with uvicorn[standard])
websockets>=12.0

# HTTP client (JWKS fetch, Translator REST fallback)
httpx>=0.26.0
